# dependencies
node_modules/
data-pipeline/.venv/
data-pipeline/__pycache__/
data-pipeline/**/__pycache__/

# next.js
.next/
out/
build/

# env
.env
.env.local
.env.*.local
data-pipeline/.env

# telegram(telethon) 세션은 StringSession으로 .env(TELEGRAM_SESSION)에 넣는다.
# 혹시 파일 기반 세션이 생겨도 커밋되지 않도록 방어적으로 무시한다.
data-pipeline/*.session
data-pipeline/*.session-journal

# claude code local settings
.claude/settings.local.json
# 에이전트 작업용 워크트리(용량이 크고 레포 내용의 사본이다)
.claude/worktrees/

# local dev-only description/details overlay (never deployed; see dev-overrides.example.json)
dev-overrides.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
